<!-- 搜索栏 -->
<view class="search-bar">
  <view class="search-input-wrap">
    <text class="search-icon">🔍</text>
    <input
      class="search-input"
      type="text"
      placeholder="搜索车手、车队、赛事"
      value="{{keyword}}"
      bindinput="onInput"
      confirm-type="search"
    />
    <view wx:if="{{keyword}}" class="clear-btn" bindtap="clearInput">
      <text>✕</text>
    </view>
  </view>
</view>

<!-- Tab切换 -->
<view class="tab-bar">
  <view
    class="tab-item {{searchType === 'riders' ? 'tab-active' : ''}}"
    data-type="riders"
    bindtap="switchTab"
  >
    <text>车手</text>
  </view>
  <view
    class="tab-item {{searchType === 'teams' ? 'tab-active' : ''}}"
    data-type="teams"
    bindtap="switchTab"
  >
    <text>车队</text>
  </view>
</view>

<!-- 搜索历史（未搜索时显示） -->
<view class="container" wx:if="{{!searched && !loading && !keyword && searchHistory.length > 0}}">
  <view class="history-header">
    <text class="history-title">搜索历史</text>
    <text class="history-clear" bindtap="clearHistory">清除</text>
  </view>
  <view class="history-list">
    <view
      class="history-tag"
      wx:for="{{searchHistory}}"
      wx:key="*this"
      data-keyword="{{item}}"
      bindtap="tapHistory"
    >
      <text>{{item}}</text>
    </view>
  </view>
</view>

<!-- 初始状态 -->
<view class="initial-state" wx:if="{{!searched && !loading && !keyword && searchHistory.length === 0}}">
  <text class="initial-icon">🔍</text>
  <text class="initial-text">搜索车手或车队名称</text>
</view>

<!-- 加载中 -->
<view class="loading-container" wx:if="{{loading}}">
  <view class="loading-spinner"></view>
  <text class="loading-text">搜索中...</text>
</view>

<!-- 错误状态 -->
<view class="error-state" wx:if="{{!loading && loadError}}">
  <text class="error-state-icon">⚠️</text>
  <text class="error-state-text">搜索失败，请检查网络</text>
  <button class="retry-btn" bindtap="retrySearch">重新搜索</button>
</view>

<!-- 搜索结果列表 -->
<view class="result-container" wx:if="{{!loading && !loadError && searched}}">
  <!-- 无结果 -->
  <view class="empty-state" wx:if="{{results.length === 0}}">
    <text class="empty-state-icon">🔍</text>
    <text class="empty-state-text">未找到相关结果</text>
  </view>
  
  <!-- 车手结果 -->
  <view wx:if="{{searchType === 'riders' && results.length > 0}}">
    <view
      class="result-card"
      wx:for="{{results}}"
      wx:key="id"
      data-id="{{item.id}}"
      bindtap="goToDetail"
    >
      <view class="tag tag-blue">{{item.nationalityZh || item.nationality}}</view>
      <view class="result-info">
        <text class="result-name">{{item.rider_name_zh || item.rider_name}}</text>
        <text class="result-sub" wx:if="{{item.rider_name_zh}}">{{item.rider_name}}</text>
      </view>
      <text class="arrow">›</text>
    </view>
  </view>
  
  <!-- 车队结果 -->
  <view wx:if="{{searchType === 'teams' && results.length > 0}}">
    <view
      class="result-card"
      wx:for="{{results}}"
      wx:key="id"
      data-id="{{item.id}}"
      bindtap="goToDetail"
    >
      <view class="tag tag-blue">{{item.uci_code || item.country || '车队'}}</view>
      <view class="result-info">
        <text class="result-name">{{item.team_name_zh || item.team_name}}</text>
        <text class="result-sub" wx:if="{{item.team_name_zh}}">{{item.team_name}}</text>
      </view>
      <text class="arrow">›</text>
    </view>
  </view>
</view>
